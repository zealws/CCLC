fun ()
  vars
    int a,
    int b,
    int c,
    int d
  in
    input a;
    input b;
    output (a + b);
    output (a - b);
    output (a * b);
    output (a / b);
    output (-a);
    output (+b)
    endvars
  endfun
